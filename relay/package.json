{
  "name": "snitch-relay",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Pairing WebSocket relay so a phone can reach a Snitch desktop dashboard across networks. Forwards end-to-end-encrypted frames only; keeps nothing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
